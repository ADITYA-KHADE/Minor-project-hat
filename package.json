{
  "name": "minor-project",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"python bert-extractive-summarizer/server.py\" \"npm run dev --prefix frontend\"",
    "server": "python bert-extractive-summarizer/server.py",
    "web": "npm run dev --prefix frontend",
    "add": "npm i --prefix frontend && npm i"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
