numpy>=1.24.0
torch>=2.0.0
spacy>=3.0.0   # Updated to a compatible version
transformers>=4.21.0  # Updated to a newer version
sentencepiece==0.1.96
tqdm
scikit-learn
pytest
sentence-transformers>=2.2.0  # Updated to the latest version

# Replace with a more recent spacy model, if possible
spacy-models>=3.0.0  # Use a compatible model version

# NeuralCoref may not work with the latest Spacy; consider alternatives
# neuralcoref==4.0  # Commented out, consider alternatives

# If using the spacy model directly, install it via pip instead of a URL
# Comment out the following line, and install the model separately
# https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-2.1.0/en_core_web_sm-2.1.0.tar.gz
